services:
  # NestJS Backend API Service
  - type: web
    name: pacs-track-backend
    runtime: node
    env: node
    region: oregon
    plan: free
    branch: main
    # Auto-deploy on every push to main branch
    autoDeploy: true
    # Build: install all deps (including dev deps for TypeScript compilation), generate Prisma client, run migrations, build backend
    buildCommand: npm ci --include=dev && npx prisma generate --schema=packages/database/prisma/schema.prisma && npx prisma migrate deploy --schema=packages/database/prisma/schema.prisma && npm run build:backend
    # Start the application
    startCommand: npm run start --workspace=@pacs-track/backend
    # Health check endpoint for monitoring
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://your-app.vercel.app
